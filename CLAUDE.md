# UMK — 자율주행 트랙 경주 로봇 (국민대 자율주행 경진대회)

## 저장소 = 실차 배포 소스
이 저장소의 각 최상위 폴더(`track_drive/`, `yolo_ros/`, `xycar_device/`)는 실제 로봇의
`~/xycar_ws/src/` 안에 들어가는 ROS2 패키지입니다. 여기서 수정 → 실차에서 pull →
해당 폴더를 `xycar_ws/src/`에 붙여넣고 `colcon build`하는 배포 모델입니다.

## 이 환경엔 ROS2 툴체인이 없음
`ros2`/`colcon` 미설치. 검증은 `python3 -m py_compile <file>`(문법 체크)까지만 가능 —
실제 빌드/노드 실행/토픽 확인은 실차에서만 할 수 있습니다.

## 튜닝값은 `track_drive/track_drive/config.py` 하나로
대부분의 모듈이 `from .config import *`로 가져다 씁니다. 파라미터를 찾거나 바꿀 땐
여기부터 확인하세요.

## 문서화 컨벤션
`track_drive/track_drive/README.md`는 번호 섹션(0~8)마다 날짜·원인·수정·"알려진 한계"를
남기는 살아있는 설계문서입니다. 코드에 기능/버그수정을 넣을 땐 같은 스타일로 README도
갱신하는 게 이 저장소의 관례입니다. 코드 주석도 "무엇"보다 "왜"(설계 배경, 실차 실측
여부, 날짜별 수정 이력)를 한글로 촘촘히 남기는 톤입니다 — 새 코드도 이 톤에 맞출 것.

## 센서 죽음은 크래시가 아니라 "조용한 고정값"으로 나타남
IMU/VESC 등 센서가 죽어있으면 콜백이 안 불려서 해당 변수(`imu_yaw`, `v_mps` 등)가
초기값/직전값에 그대로 고정됩니다 — 에러 로그 없이 "이상하게 값이 안 움직인다"는
증상으로만 드러나니 주의. 이 저장소는 `_vesc_live()` 같은 `_*_t` 타임스탬프 +
`*_STALE_SEC` 가드 패턴으로 생존을 체크합니다 — 새 센서값을 쓸 때도 이 패턴을 따를 것.

## da(주행가능영역) 블롭 선택은 면적 임계값만으로 하지 말 것
`_largest_da_component()`(`perception/dl_lane.py`)가 da 커넥티드컴포넌트 중 "내 차선"을
고를 때, 픽셀 면적 범위(`DL_DA_MIN_COMPONENT_AREA`/`DL_DA_MAX_AREA_PX`)만으로 판단하는
방식은 실차 주행 + 파라미터 재조정으로도 안 통했습니다(2026-08-07 확인, README §2.11).
대신 물리적 위치 근거("시드" — 차량 위치와 맞닿은 덩어리)와 시간적 연속성(직전 프레임과
가장 가까운 덩어리)을 우선 기준으로 쓰고, 면적은 그 둘 다 없을 때의 최후 폴백 정도로만
남겨둘 것.

## Git 커밋/푸시 자동화 범위 (2026-08-11)
- **`main`이 아닌 브랜치**(feature/작업 브랜치)에서는 변경사항에 적절한 커밋 메시지를 작성해
  커밋하고, 곧바로 그 브랜치로 push까지 진행해도 됩니다 — 매번 채팅에서 "push해도 될지"
  다시 확인받을 필요 없음.
- **`main`으로의 push, force-push, 브랜치 삭제**는 이 예외에서 제외됩니다 — 여전히 매번
  채팅에서 명시적으로 확인받을 것. 이 저장소는 실차 배포 소스라(위 "저장소 = 실차 배포
  소스" 참고) main이 그대로 실차에 반영되는 경로라서 신중해야 합니다.
- 이 지침은 "채팅으로 다시 물어보는 절차"만 생략하는 것이고, 터미널의 git push 실행 자체에
  대한 도구 승인 프롬프트는 별도로 그대로 유지됩니다(사용자가 명시적으로 원치 않음).

## `xycar_ws` ↔ UMK 저장소 동기화 (실차에서, 사용자명 `xytron`일 때) (2026-08-21)
실행 중인 OS 사용자명이 **`xytron`**이면(= 지금 이 세션이 실제 로봇 위에서 직접 도는 중이라는
뜻 — 위 "저장소 = 실차 배포 소스" 참고) 아래 양방향 동기화를 매번 다시 물어보지 않고 자동으로
수행합니다:
- **UMK → xycar_ws**: UMK 저장소에서 최신을 pull 받으면, 바뀐 최상위 패키지 폴더
  (`track_drive/`, `yolo_ros/`, `xycar_device/`)를 그대로 `~/xycar_ws/src/`의 같은 폴더에
  복사(덮어쓰기)해 실차 배포본에도 반영합니다.
- **xycar_ws → UMK**: `~/xycar_ws/src/` 안의 패키지 폴더를 실차에서 직접 수정했다면, 그
  변경분을 UMK 저장소의 같은 경로로 복사해 반드시 반영합니다 — 실차에서만 고치고 UMK에는
  안 남기는 일이 없도록 할 것.
- 두 방향 다 반영한 뒤에는 가능하면 커밋 메시지를 작성해 커밋하고, UMK 원격 브랜치로 push까지
  자동으로 진행합니다 — 위 "Git 커밋/푸시 자동화 범위" 절의 예외(main push/force-push/브랜치
  삭제는 여전히 확인 필요, git push 자체의 도구 승인 프롬프트도 유지)는 이 동기화에도 그대로
  적용됩니다.

## da가 옆 차선과 완전히 병합됐을 때 "침식(erosion)"이 항상 통하진 않음
ll(차선) 인식이 프레임 전체에서 실패하면(예: `ll_bands:0/8`), da가 두 차선을 잇는 지점이
"얇은 다리"가 아니라 뭉텅하게 넓은 채로 병합되는 경우가 실차 캡처로 확인됐습니다
(2026-08-07, README §2.14) — da 세그멘테이션 모델 자체가 그 프레임에서 두 차선을
시각적으로 구분 못 한 것이라, 침식으로 끊어낼 얇은 구조 자체가 없습니다. 이 실패모드는
ll 잔상(decay, 최근 프레임의 확실했던 픽셀을 감쇠 유지)과 기대 차로폭 기반 가상경계
(증거 없이 강제 클리핑)로 대응하는 게 낫습니다 — `perception/dl_lane.py`
`_clip_da_by_ll()` 참고.
