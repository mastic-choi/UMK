# 실제속도측정 — 작업 요약 + 이어서 할 일 (PC 재시작 대비 백업, 2026-08-17)

이 파일은 PC 재시작 전에 지금까지 대화 맥락과 만든 도구를 한 곳에 백업해둔 것입니다.
**`measure_speed_calibration.py` 원본 자체는 이미 실차 소스 위치(아래 코드블럭과 동일 경로)에
정상 저장돼 있으니 재시작해도 사라지지 않습니다** — 이 md는 "다음 세션에 Claude가 맥락을
빠르게 복구하기 위한" 백업 겸 작업 지시서입니다.

## 0.1 2026-08-18 밤 세션 — 하드웨어 신뢰성 급격히 악화, 25 측정 보류

**⚠ 가장 중요한 사실: 이날 세션 후반, 완충 상태인데도 30분에 1번꼴로만 정상 출발**(이전
세션엔 5번 중 못해도 2번은 출발했음 — 명백한 악화). **소프트웨어 가설 두 개가 모두 실차에서
기각됨**([[project_track_drive_startup_stutter]] 메모리 참고, 자세한 내용은 그쪽이 최신·
최우선):
- `SPEED_KICK_START=3.0`(정지마찰 스틱션 킥) 실차 적용 후에도 틱틱거림 개선 체감 전혀 없음.
- 좀비 `dynamic_bridge` 제거([[motor_bridge_zombie_fix]])도 그때 증상 호전이 우연이었을
  가능성이 크다고 재평가됨.
- 즉 남은 유력 용의선은 **하드웨어**(배터리 내부저항 열화/ESC 보호임계값 이상/모터·기어박스
  기계적 걸림/배터리-ESC 커넥터 접촉불량) — 다음 세션에서 **다른 정상 배터리팩으로 교체해
  출발 성공률 A/B 비교**하는 게 가장 빠른 다음 진단 스텝으로 제안됨. **원인 못 찾으면
  speed=25 실측은 계속 미룰 것** — 지금 상태로 반복 측정 시도하는 건 부품에도 안 좋음.

**도구 개선(`measure_speed_calibration.py`):**
- **mark2 자동 기록** 추가(§3.5 2마커 프로토콜 지원) — 사람이 mark0(출발)·mark1(정속 진입)
  두 번만 Enter를 누르면, `--max-duration` 컷오프 0.3초 전에 코드가 자동으로 mark2를
  기록하고 자동 정지까지 처리한다(`add_marker()`, `AUTO_MARK2_LEAD_SEC=0.3`, 패시브모드는
  기존 방식 그대로 유지). 사람이 컷오프 타이밍을 직접 맞추다 감속이 섞이거나 너무 일찍
  찍는 실수를 없애기 위함.
- **vesc_debug 창을 추가했다가 다시 제거함** — track_drive.py의 `DEBUG_VIZ_VESC` 창을
  이식해서 "출발 못하고 울컥거릴 때 VESC가 LIVE인데 0인지" 실차에서 직접 확인해봤는데,
  **울컥거리는 순간에도 계속 초록(LIVE)으로만 뜸을 확인**. 재검토 결과 이건 당연한
  결과였다 — 바퀴가 진짜로 안 도는 순간엔 원인(배터리 LVC 트립이든 §7.2 정지마찰/
  락터전류 스틱션이든)과 무관하게 VESC가 v_mps=0을 "정직하게" 보고하므로, 이 창은
  토픽 자체가 죽었는지(주황/빨강)만 가려줄 뿐 파이프라인이 멀쩡한 채로 진짜 안 도는
  케이스의 세부 원인은 구분해주지 못함 — 진단 가치가 없다고 판단해 코드 제거.

**speed=20 재측정 시도(2026-08-18 밤, §3.5 신규 2마커 프로토콜) — 총 10여 회 시도 중
성공 1회, 그마저도 대표값으로 채택 보류:**

| drive-speed | 구간 | dt | 실측거리 | real_mps | ctrl_avg(가중) | unit | 비고 |
|---|---|---|---|---|---|---|---|
| 20 | mark1↔mark2 | 5.386s | 11.86m | 2.202 | 19.05 | **0.1156** | VESC오차 30.8%(기존 4~11%보다 훨씬 큼) |
| 20 | mark0→컷오프 전체(7.0s, 가속 포함) | 7.0s | 11.86m | 1.694 | 16.46(가중) | 0.1029 | 참고용(가속구간 섞임) |

**⚠ 이 값들은 버리지 않고 보류 상태로 남겨둠(사용자 지시) — 기존 대표값(0.072, ①~⑤회차
풀링)을 대체하지 않음.** 이 run이 나온 시점이 하드웨어가 극도로 불안정하던 구간(위 참고)과
겹치고, VESC오차도 이례적으로 커서(30.8% vs 기존 4~11%) 대표성이 의심스러움. 기존 추세
(5→10→15→20 갈수록 unit이 평평하다가 20에서 하락, 0.085→0.072)와 정반대 방향(0.116/0.103
으로 오히려 급등)이라 더더욱 그대로 못 씀.

**다음 계획(사용자 지시) — 새 측정 방식 자체를 검증할 것:** 오늘 20에 쓴 새 2마커 프로토콜
(사람이 mark0·mark1만 찍고 mark2는 자동)로 **5·10·15도 다시 재측정**해서, 기존 방식(35cm
간격 여러 마크 + 회귀, 5→0.085/10→0.086/15→0.085)으로 나온 값과 얼마나 일치하는지 확인할
것. 일치하면 "새 방식 자체는 믿을 만하다"가 확인되는 것이고, 안 맞으면 오늘 20 이상치가
하드웨어 문제가 아니라 새 방식(mark1을 사람이 직접 판단하는 타이밍)의 오차일 가능성이
커짐 — 이 교차검증이 끝나야 20/25 실측을 신뢰할 수 있는 상태가 됨.

## 0. 현재 상태 요약 (2026-08-18, 배터리 부족 + PC 재시작으로 세션 중단)

- 완료: speed **5·10·15·20**의 대표값 확보(§4.5 표, 20은 §4.5.1 통합재분석으로 갱신).
  5~15는 unit≈0.085~0.086으로 거의 완벽히 선형/무데드존. **20은 재분석 결과 unit≈0.072로
  약 15~16% 낮아짐 — 고속 견인력 한계(포화) 시작 가능성에 무게가 실림**(§4.5.1).
- **남은 일: speed 25만 측정하면 실측 데이터 수집 끝** — 그 다음 5개 점(5/10/15/20/25)으로
  최종 회귀 돌려서 §4의 "다음에 Claude가 해야 할 일"(config.py `METERS_PER_SPEED_UNIT`
  갱신 + README §6.5 갱신) 진행하면 이 작업 전체가 마무리됨.
- **⚠ 배터리 이슈**: 이 세션에서 능동모드 주행을 총 약 20분(15회 이상 구동) 진행한 뒤
  사용자가 "배터리가 모자란 듯하다"고 보고, 세션 중단. speed=20의 4·5회차(23:45, 23:47)가
  유독 노이즈 크고 실측속도가 낮게 나왔던 것을 "사람 반응시간 노이즈"로 해석했는데(§4.7),
  **배터리 소모로 인한 출력 저하일 가능성도 재검토할 것** — 다만 그 두 run의 `vesc_avg`는
  이전 run들과 거의 동일(≈1.58~1.60)했으므로 배터리 전압 드롭이 원인이라면 VESC 판독값도
  같이 떨어졌어야 하는데 그러지 않았음 — 완전히 들어맞진 않지만 배제하기도 이름.
  **다음 세션은 배터리를 완충한 뒤 재개할 것을 권장.**
- **speed=25 측정 방법(사용자와 합의된 새 프로토콜, 아래 §3.5 참고) — 기존처럼 40cm~1.4m
  간격으로 여러 번 Enter 누르는 대신, 딱 2번만 마크한다:**
  1. `python3 measure_speed_calibration.py --drive-speed 25 --interval-m 1.0`
     (`--interval-m` 값은 이번 방식에선 안 씀 — 아무 값이나 둬도 무방, 무시하고 계산할 것)
  2. Enter → 출발(mark0, 가속 시작)
  3. **가속 끝나고 정속 됐다 싶은 순간 Enter → mark1.** 이 순간 물리적 위치를 표시해둘 것
     (옆 사람이 그 순간 물건 떨어뜨리기 등).
  4. `--max-duration`(기본 10초) 걸리기 살짝 전에 **한 번 더 Enter → mark2**(감속 시작 전에
     눌러야 정속구간이 안 오염됨). 애매하면 Ctrl+C로 즉시 정지해도 됨. 이 위치도 표시.
  5. 차 세운 뒤 **mark1↔mark2 사이 실측 거리를 줄자로 재서** Claude에게 알려줄 것.
  6. **계산 방법**: 콘솔/CSV의 segment2(mark1→mark2) `dt`는 두 Enter 사이 실제 시간차라
     `--interval-m` 가정과 무관하게 정확함 — 이 `dt`와 사용자가 알려준 진짜 거리로
     `real_mps = 진짜거리 / dt`를 다시 계산할 것(표에 찍힌 `dd`/`real_mps`는 잘못된
     interval 가정 기반이라 무시). `ctrl_avg`/`vesc_avg`는 그 구간 자동평균이라 그대로
     신뢰 가능. 이 방식은 길게(수 초) 한 구간으로 재는 거라 반응시간 오차 비중이 작아져서
     20에서 겪은 반응시간 노이즈 문제를 줄여줄 것으로 기대.
- 다음 세션 시작 시 순서: ① 배터리 완충 확인 → ② speed=25를 위 새 프로토콜로 측정 →
  ③ (선택) 완충 상태로 speed=20 재검증 1~2회(배터리 가설 확인용) → ④ §4의 "다음에
  Claude가 해야 할 일" 진행(최종 회귀로 config.py/README 갱신).

## 1. 배경/목적

- `config.py`의 `METERS_PER_SPEED_UNIT = 0.1347`(모터 커맨드 speed 단위 → m/s 환산)은
  README.md §6.5에 "speed=5/10 단 2점 실측(각 2회)만으로 낸 회귀값, 2점 외삽 한계"라고
  명시돼 있는 추정치. x절편(모터 데드존 추정치 speed≈1.4)도 실측이 아니라 추정.
- VESC 홀센서 실측값(`v_mps`, README §7, `/vesc_speed_erpm` 토픽)도 지금까지 줄자 등
  지상 실측으로 교차검증된 적이 없음.
- 사용자가 줄자로 직접 실제 지상속도를 재서, ① 커맨드 speed→m/s 환산이 맞는지
  ② VESC 실측이 실제와 얼마나 맞는지 둘 다 검증하기로 함.

## 2. 지금까지 한 일

1. `xycar_ws/src/track_drive/track_drive/measure_speed_calibration.py` 작성(전체 코드는
   아래 §5). `measure_lidar_camera_offset.py`와 같은 관례로 `track_drive` 패키지를
   import하지 않는 독립 실행 스크립트 — colcon build 없이 바로 `python3` 실행 가능.
   `python3 -m py_compile`로 문법 체크 완료(이 환경엔 ROS2 툴체인이 없어 실행 자체는
   실차에서만 검증 가능 — CLAUDE.md 관례).
2. 핵심 동작: 트랙에 줄자로 등간격 지점을 표시 → 차량이 각 지점을 지날 때 Enter로
   타임스탬프 기록 → 구간(mark[i-1]→mark[i]) 실제속도 = 구간거리/구간시간. 그 시간창
   동안 로깅해둔 `xycar_motor`(ctrl_speed)/`/vesc_speed_erpm`(v_mps) 평균과 비교.
3. 두 모드 지원:
   - **패시브**(기본): 이미 떠 있는 `track_drive.py` 등이 발행하는 `xycar_motor`를 구독만.
   - **능동**(`--drive-speed X`): 이 도구가 직접 `angle=0, speed=X`를 발행해 직진 정속
     테스트를 만듦(가속 램프는 `config.py SPEED_ACCEL_STEP`과 동일하게 재현). ★완전
     개루프라 장애물/차선 인식 없음★ — `track_drive.py`는 반드시 꺼두고, 사람 없는
     직선 트랙에서만, Ctrl+C로 즉시 정지 가능, `--max-duration`(기본 10초) 지나면
     자동으로 speed=0.
4. 사용자가 "40cm마다 Enter 눌러도 되냐"고 물어서 다음 두 가지를 보강:
   - `--interval-m` 기본값을 2.0 → **0.4(40cm)**로 변경.
   - "첫 구간만 가속구간"이라는 하드코딩을 없애고, **연속 2구간 실제속도가 12% 이내로
     수렴하는 지점부터 steady로 보는 자동감지**(`_detect_steady_start()`)로 교체 — 40cm
     간격이면 가속이 여러 구간에 걸칠 수 있어서.
   - steady 구간의 각 Enter 시점(t, 누적거리)들을 **최소자승 회귀**로 묶어 잡음에 강한
     속도값을 추가로 계산(`_regression_speed()`) — 개별 구간(dt가 0.2~0.3초 수준이라
     사람 반응시간 오차 비중이 큼)보다 이 값을 최종 수치로 신뢰하도록 안내.
5. `--drive-speed` 단위는 m/s가 아니라 config.py의 speed 커맨드 단위(0~100)임을 설명.
   테스트 추천 값: **5(저속/데드존 확인용), 10, 15(=현재 SPEED_NORMAL), 20, 25** —
   특히 5는 README §6.5의 데드존 추정치(speed≈1.4)를 실측으로 검증하기 위해 필수.
   각 speed당 2~3회 반복 측정을 권장(사람 반응시간 잡음 평균으로 상쇄).

## 3. 실행법 (요약)

```bash
cd ~/xycar_ws/src/track_drive/track_drive

# 능동 모드 — 이 도구가 직접 직진 정속 주행을 만듦(track_drive.py는 꺼둘 것)
python3 measure_speed_calibration.py --drive-speed 15
# --interval-m 기본 0.4(40cm)라 생략 가능. 다른 speed로 반복:
python3 measure_speed_calibration.py --drive-speed 5
python3 measure_speed_calibration.py --drive-speed 10
python3 measure_speed_calibration.py --drive-speed 20
python3 measure_speed_calibration.py --drive-speed 25

# 패시브 모드 — track_drive.py가 이미 돌고 있을 때 관찰만
python3 measure_speed_calibration.py
```

절차: 차량을 0m 지점에 두고 실행 → "준비되면 Enter" → Enter(능동모드면 즉시 출발) →
40cm 표시 지점 지날 때마다 Enter → 다 지났으면 `q` + Enter로 종료 → 콘솔에 구간별
결과표 + steady-state 요약(회귀 기반 실제속도, 회귀 기반 METERS_PER_SPEED_UNIT, VESC
오차%) 출력 + `speed_calib_logs/`에 CSV 자동 저장.

## 4. 다음에 Claude가 해야 할 일 (사용자가 실측 결과를 다시 붙여넣으면)

1. 사용자가 여러 `--drive-speed` 값(5/10/15/20/25 등)으로 돌린 콘솔 출력(또는 CSV)을
   받으면, 각 실행의 "steady-state 요약" 중 **회귀 기반 steady 실제속도**와 **ctrl_speed
   평균**을 (speed, real_mps) 점 목록으로 모은다.
2. 이 점들로 새 선형회귀를 돌려 `METERS_PER_SPEED_UNIT`을 다시 추정하고, x절편(데드존
   추정 speed)도 다시 계산한다 — 기존 2점(5,10) 회귀보다 점이 늘어난 만큼 신뢰도가
   올라가는지, 혹은 저속 구간(특히 5)에서 비선형(데드존 근처 곡선)이 드러나는지 확인.
3. VESC(`v_mps`) 평균 오차%도 같이 취합해서, `v_mps`를 "실측"으로 신뢰하고 앞으로는
   줄자 없이 VESC만으로 캘리브레이션해도 되는 수준인지(오차가 충분히 작다면) 판단.
4. 결론이 나오면:
   - `config.py`의 `METERS_PER_SPEED_UNIT` 값과 그 위 주석(실측 날짜/방법/샘플수, 이
     저장소 관례대로 "★실측 필요★" 표시 제거 + 실측 근거 명시)을 갱신.
   - `README.md` §6.5를 갱신(날짜·원인·수정 스타일의 이 저장소 문서화 관례 그대로,
     "2점 외삽 한계" 문구를 실측 완료 상태로 교체).
   - 데드존 재추정이 `SPEED_CORNER_MIN`/`SPEED_LL_DEGRADED`/`SPEED_LANE_STALE`(현재
     5.0으로 설정된 값들) 대비 여전히 안전한 위치인지도 같이 확인해 필요하면 언급.
5. 만약 결과가 "여전히 노이즈가 커서 판단 불가" 수준이면, 반복 횟수를 늘리거나
   `--interval-m`을 넓혀서(예: 1.0m) 재측정을 제안할 것.

## 4.5. 수집된 실측 데이터 (누적, 세션 재시작 대비)

새 결과가 들어올 때마다 이 표에 행을 추가할 것. §4의 "다음 할 일"은 이 표가 다 채워진
뒤(권장: speed 5/10/15/20/25 각 반복 포함) 진행.

| drive-speed(cmd) | ctrl_speed avg | 회귀기반 steady 실제(m/s) | 회귀기반 unit(m/s per cmd) | VESC 평균오차% | 비고 |
|---|---|---|---|---|---|
| 5 | 5.00 | 0.431 | 0.0862 | 7.4% (VESC가 실제보다 낮게 측정) | 2026-08-17 22:46 run(1회차). steady n=10, dt 0.77~0.86s(짧아서 개별구간 잡음 큼, 회귀값 신뢰). 표준편차 0.016(3.7%)로 낮아 steady 잘 잡힘. **기존 cfg(0.1347) 기준 pred=0.673m/s인데 실측 0.431m/s — 기존 2점회귀가 저속 구간을 크게 과대추정하고 있었음을 시사.** |
| 5 | 5.00 | 0.429 | 0.0858 | 8.5% (VESC가 실제보다 낮게 측정) | 2026-08-17 22:48 run(2회차). steady n=10, dt 0.72~0.95s. 1회차와 거의 동일(0.431→0.429, 오차 0.5% 이내) — **speed=5 재현성 확인됨, 노이즈 아니라 실제값.** |
| 5 | 5.00 | 0.420 | 0.0839 | 6.1% (VESC가 실제보다 낮게 측정) | 2026-08-17 22:49 run(3회차). steady n=6, dt 0.78~0.93s. segment1 dt=4.586s로 유독 길었음 — **사용자 보고: 출발 시 모터가 정지마찰을 못 이겨 틱틱거리며 못 나가는 현상 관찰**(가속구간으로 자동제외되어 steady엔 영향 없음). 정성적으로 speed=5가 데드존 경계에 가깝다는 걸 뒷받침하는 정황. |
| **5(3회 평균)** | **5.00** | **≈0.427** | **≈0.085** | **≈7.3%** | 3회 run 평균(0.431/0.429/0.420, 표준편차 0.006로 매우 낮음 — 안정적). speed=5의 최종 대표값으로 채택. |
| 10 | 10.00 | 0.858 | 0.0858 | 7.0% (VESC가 실제보다 낮게 측정) | 2026-08-17 22:54 run(1회차). steady n=20(마크 21개), dt 0.35~0.44s(매우 짧음 — 개별구간 잡음 큼, 회귀값 신뢰 필수). 표준편차 0.055(6.4%). |
| 10 | 10.00 | 0.859 | 0.0859 | 8.3% (VESC가 실제보다 낮게 측정) | 2026-08-17 22:56 run(2회차, `--drive-speed 15`를 의도했으나 실제로는 10으로 실행된 것으로 보임 — 파일명/ctrl_avg 모두 10). 1회차와 사실상 동일(0.858→0.859) — **speed=10도 재현성 확인됨.** |
| 10 | 9.87 | 0.859 | 0.0870 | 7.8% (VESC가 실제보다 낮게 측정) | 2026-08-17 23:30 run(3회차, `--interval-m 0.35` 사용, 23구간). steady n=19(마크20개), dt 0.36~0.45s. segment23에서 `--max-duration`(기본10.0s) 컷오프 근접(총주행시간≈10.1s)으로 ctrl_avg가 7.50으로 급락하는 이상치 1개 있었으나 steady 19개 중 1개뿐이라 회귀값엔 거의 영향 없음. 1·2회차와 사실상 동일(0.858/0.859→0.859) — 3회 연속 재현. |
| 10 | 10.00 | 0.864 | 0.0864 | 10.9% (VESC가 실제보다 낮게 측정) | 2026-08-17 23:32 run(4회차, `--interval-m 0.35`). steady n=17(마크18개, segment7부터 — 가속구간이 seg1~6까지 다소 길게 잡힘, 초반 실제속도가 0.343→0.796→1.022→0.765→0.930→0.696로 튐이 커서 steady 수렴점 판정이 다소 늦음). 실제속도 표준편차 0.087(10%)로 이전 회차보다 노이즈 큼 — 그럼에도 회귀값(0.864)은 이전 3회(0.858/0.859/0.859)와 0.7% 이내로 근접. VESC오차만 10.9%로 이전 회차들(7.0~8.3%)보다 다소 높음 — VESC오차가 speed=10에서도 7~11% 범위로 약간 흔들린다는 뜻. |
| **10(4회 평균)** | **9.97** | **≈0.860** | **≈0.0863** | **≈8.5%** | 4회 run 평균(0.858/0.859/0.859/0.864, 표준편차 0.003으로 매우 낮음 — 매우 안정적). speed=10의 최종 대표값으로 채택(기존 2회 평균에서 갱신). |

| 15 | 15.00 | ~~0.624~~ **1.248**(보정) | ~~0.0416~~ **0.0832**(보정) | ~~96.1%~~ **≈3.5%**(보정) | 2026-08-17 23:33 run(1회차). **버그: 실제 줄자 마킹은 70cm였는데 `--interval-m` 플래그가 0.35로 그대로 실행됨**(쉘 히스토리 재사용 추정) — 스크립트가 거리를 절반으로 계산해서 real_mps/unit이 전부 실제의 절반, VESC오차는 반대로 뻥튀기됨. real_mps·unit·VESC오차 전부 ×2/재계산 보정값으로 기재. steady n=9(마크10개). |
| 15 | 15.00 | ~~0.632~~ **1.264**(보정) | ~~0.0422~~ **0.0844**(보정) | ~~90.2%~~ **≈4.2%**(보정) | 2026-08-17 23:35 run(2회차). **동일 버그 재발**(플래그 재수정 안 됨, 사용자 확인). steady n=14(마크15개). 1회차 보정값(1.248)과 거의 동일(1.264) — 보정 후 재현성 확인됨. |
| 15 | 14.52 | 1.281 | 0.0883 | 7.2% (VESC가 실제보다 낮게 측정) | 2026-08-17 23:38 run(3회차, **`--interval-m 0.7` 정상 실행** — 보정 불필요, 직접 측정값). steady n=16(마크17개). segment17에서 `--max-duration`(기본10.0s) 컷오프 근접(총주행시간≈10.2s)으로 ctrl_avg 8.09로 급락하는 이상치 1개 있었으나 영향 미미. 1·2회차 보정값(1.248/1.264)과 잘 일치(1.281) — **보정 방법론이 이 정상측정으로 교차검증됨.** |
| **15(3회 평균, 1·2회는 보정값)** | **~14.8** | **≈1.264** | **≈0.0853** | **≈5.0%** | speed=15의 최종 대표값으로 채택. unit이 speed=5(0.085)·10(0.086)와 0.083~0.088 범위로 거의 일정 — **선형/무데드존 가설이 3개 속도점(5·10·15)에서 계속 재확인됨.** |

| 20 | ~~19.53~~ | ~~1.385~~ | ~~0.0709~~ | ~~7.2%~~ | 2026-08-17 23:41 run(1회차, `--interval-m 1.05`). **⚠ 참고용/제외 — 사용자 보고: 주행 중 갑자기 확 감속했다가 다시 급가속하며 치고나가는 현상 발생**(§4.7 참고). steady 6개 구간 평균에 이 감속/재가속 구간이 섞여 unit이 0.071로 5·10·15(0.083~0.086)보다 눈에 띄게 낮게 나옴 — 측정 노이즈가 아니라 실제 그 현상이 값을 오염시킨 것으로 판단, 이 값 자체는 최종 대표값 산정에 사용하지 않음. 재측정 필요. |
| 20 | ~~18.57~~ | ~~1.303~~ | ~~0.0701~~ | ~~21.4%~~ | 2026-08-17 23:42 run(2회차, `--interval-m 1.05`). **⚠ 참고용/제외 — 같은 현상 재발**, 다만 이번엔 segment5(dt=1.133s, real=0.927)로 원인 단서를 남김(§4.7 "결정적 단서" 참고 — ctrl_avg/vesc_avg는 안 변했는데 real_mps만 하락 → traction loss 정황). 이 값도 최종 대표값 산정 제외, 사용자가 마크 간격 불균일 구간을 걸러내고 재측정 예정. |
| 20 | 19.81 | 1.581 | 0.0798 | 4.2% (VESC가 실제보다 낮게 측정) | 2026-08-17 23:44 run(3회차, `--interval-m 1.05`). **가장 깨끗한 측정 — 이상현상 없음**, steady 12구간 전부 dt 0.6~0.7s로 균일, ctrl_avg 20.00 고정. unit=0.0798로 5·10·15(0.085~0.086)보다 살짝 낮음. **speed=20 현재 대표값으로 채택**(아래 4회차는 노이즈+이상현상으로 참고용). |
| 20 | ~~19.06~~ | ~~1.359~~ | ~~0.0713~~ | ~~16.2%~~ | 2026-08-17 23:45 run(4회차, `--interval-m 1.05`). **⚠ 참고용/제외 — segment5에서 다시 감속 이상현상**(real 1.215, dt=0.864s로 이웃보다 김) + steady 자동감지가 segment2(ctrl_avg 17.92, 아직 가속 중)를 오분류 + segment12는 `--max-duration` 컷오프 근접으로 오염. 이 셋(2,5,12) 제외한 클린 구간(3,4,6~11) 평균 real≈1.394 m/s, unit≈0.070 — 그래도 3회차(1.581)보다 12% 낮고 dt 스프레드도 더 넓음(0.696~0.840s vs 3회차 0.629~0.736s) → **speed=20에서 반응시간 노이즈가 커졌다는 사용자 판단과 일치, 최종값엔 미반영.** |

| 20 | ~~19.03~~ | ~~1.104~~ | ~~0.0580~~ | ~~41.3%~~ | 2026-08-17 23:47 run(5회차, `--interval-m 1.05`). **⚠ 폐기 — 이번엔 국소 이상이 아니라 거의 전 구간(dt 0.79~1.13s, 3회차의 0.63~0.74s보다 전반적으로 김)에서 고르게 실측속도가 낮게 나옴**. ctrl_avg(20.00)/vesc_avg(1.58~1.60)는 이전 run들과 동일한데 real_mps만 전체적으로 하락 → 사람 반응시간 노이즈로 잠정 판단했었으나, 아래 §4.5.1 풀링 분석으로 대체됨(전체 폐기 대신 재활용). |
| **20 — ①~⑤회차 통합 재분석(§4.5.1)** | ≈20.00 | **≈1.449** | **≈0.0724** | **≈9.1%** | run 통째로 버리는 대신, ①~⑤회차 전체에서 `ctrl_avg≥19.5`(가속/컷오프 제외) AND `dt≤0.85s`(국소 이상치 제외) 세그먼트만 풀링(총 32개, 거리/시간 가중평균) — **speed=20의 최종 대표값으로 이걸로 교체**. 근거는 §4.5.1 참고. |

## 4.5.1. speed=20 통합 재분석 (2026-08-18, run 전체 폐기 대신 세그먼트 풀링)

기존엔 3회차(23:44)만 "이상현상 없는 유일한 run"으로 대표값(1.581 m/s, unit 0.0798) 삼았는데,
사용자 제안으로 ①~⑤회차 전체에서 세그먼트 단위로 재검토함.

**필터 기준:** `ctrl_avg≥19.5`(가속 램프/`--max-duration` 컷오프 구간 제외) AND `dt≤0.85s`
(국소 이상치 제외 — §4.7에서 확인된 "5m 지점 슬립"류 이벤트, 그리고 반응시간 지연으로
보이는 것들).

**런별 평균(필터 통과 세그먼트, 거리/시간 가중):**

| run | 통과 세그먼트 수 | 평균 실제속도(m/s) |
|---|---|---|
| 1회차(23:41) | 5 | 1.371 |
| 2회차(23:42) | 7 | 1.399 |
| 3회차(23:44) | 11 | **1.587** |
| 4회차(23:45) | 8 | 1.389 |
| 5회차(23:47) | 1 | 1.326 |

**핵심 발견: 3회차가 대표값이 아니라 이상치였다.** 나머지 4개 run은 1.33~1.40 m/s로
모여있는데 3회차만 눈에 띄게 빠름 — 지금까지 "가장 깨끗한 run"이라 믿고 대표값으로
썼던 게 오히려 통계적으로는 outlier였을 가능성이 큼.

**32개 세그먼트 전체 풀링(총거리 33.6m / 총시간 23.197s):**
- 실제속도 = **1.449 m/s**
- unit = **0.0724**
- vesc_avg는 run 상관없이 거의 전부 1.58~1.60으로 매우 일정 → VESC 대비 오차
  **≈9.1%**, 이는 5·10·15에서 확인된 정상 범위(4~11%)에 정확히 부합 — 이 풀링값이
  3회차 단독값(VESC오차 4.2%, 비정상적으로 낮았음)보다 신뢰도가 높다는 교차검증.

**물리적 해석:** VESC(모터 축 회전 기반)가 run에 상관없이 거의 고정값(1.58~1.60)으로
나온다는 건 모터는 명령을 정직하게 수행했다는 뜻인데, 그게 지면 이동으로 온전히
전환되지 않고 run마다/세그먼트마다 들쭉날쭉했다는 건 — **전류제한/배터리보다 타이어
그립 부족(고속 직진 시 간헐적 슬립)이 원인일 가능성에 더 무게가 실림.**

**결론(수정):**

| speed | unit(m/s/cmd) |
|---|---|
| 5 | 0.085 |
| 10 | 0.086 |
| 15 | 0.085 |
| **20(재분석)** | **0.072** |

5~15는 평평한데 20에서 **약 15~16% 낮아짐** — 이전엔 "노이즈인지 불확실"이었지만 이
정도 낙폭과 4/5 run의 일관성을 보면 **고속 견인력 한계(포화)가 실제로 시작된다는 신호로
보는 게 더 타당함.** 25 측정 결과가 이 추세(더 낮아짐)를 따라가는지가 최종 확인 포인트.

**speed=5 vs speed=10 비교 — 데드존 재평가 필요:**
- unit값이 사실상 동일(0.085 vs 0.0858) → **speed 5~10 구간이 이미 선형이고 데드존이 없거나
  매우 작다는 뜻**. 두 점(5, 0.427)·(10, 0.858)로 회귀하면 기울기≈0.0862, x절편(데드존
  추정)≈**0.05** — 기존 README §6.5의 데드존 추정치(speed≈1.4)와 크게 다름. 기존 2점회귀
  (0.1347)가 저속을 과대추정했던 이유가 바로 이 데드존 과대추정 때문이었을 가능성이 높음.
- VESC 오차도 7.3%→7.0%로 거의 동일 → 시스템적 편향(과소측정) 재확인, 속도에 무관하게
  일정 비율로 어긋나는 듯(ERPM gain 오차 등 배율성 오차일 가능성 ↑, 오프셋성 오차라면
  비율이 아니라 절대값이 비슷했을 것).
- **단, 아직 2점뿐이라 "완전 선형"이라 단정하긴 이름 — 15/20/25에서도 unit이 비슷하게
  유지되는지, 아니면 고속에서 다른 비선형(포화 등)이 나타나는지 확인 필요.**

## 4.7. 반복 관찰되는 "급감속→급가속" 현상 (speed=15, 20에서 재현, 원인 미확인)

사용자가 실차 주행 중 육안으로 **speed=15 run 때 최소 1회, speed=20 run(23:41)에서 다시**
같은 패턴을 보고함: 정속 주행 중이던 차량이 갑자기 확 느려졌다가, 곧이어 급가속하며
치고 나가는 현상. speed=20 run에서는 이 현상이 섞인 걸로 보이는 steady 평균이 unit≈0.071로
나와 다른 속도들(0.083~0.086)보다 뚜렷이 낮게 나왔음 — 즉 측정 노이즈가 아니라 실제
차량 거동이 값을 왜곡시킨 것으로 판단, 그 run은 최종값 산정에서 제외(§4.5 표 참고).

**아직 원인 불명 — 다음에 이 현상이 다시 나오면 다음을 확인/기록할 것:**
- 매번 트랙의 같은 물리적 지점(코너 진입부, 노면 이음새 등)에서 발생하는지, 아니면
  시간/거리 기준(예: 출발 후 일정 시간 경과, 가속 완료 직후)으로 발생하는지 — 전자면
  노면/트랙 문제, 후자면 모터/제어 쪽(예: VESC 전류 제한, PID 오버슈트 후 보정) 문제일
  가능성.
- speed가 높을수록(15→20) 더 자주/뚜렷하게 나타나는지 — 그렇다면 고속에서 VESC 전류
  제한(`vesc.yaml` 관련 설정)이나 배터리 전압 드롭으로 순간적으로 토크가 부족해졌다가
  회복되는 현상일 가능성 검토.
- 이 도구(`measure_speed_calibration.py`)의 능동모드는 완전 개루프 직진이라 차선/조향
  피드백과 무관하게 발생한 것 — 즉 `track_drive.py`의 제어 로직 버그가 아니라 모터/VESC
  하드웨어 또는 config.py의 가속 램프(`SPEED_ACCEL_STEP`)/전류 설정 쪽 원인일 가능성이
  더 높다는 뜻(제어 로직이 개입할 여지가 없는 순수 하드웨어 모드에서 재현됐으므로).
- 재현되면 그 구간의 raw CSV(`speed_calib_logs/`)를 남겨서 dt/ctrl_avg/vesc_avg 추이를
  시간순으로 그려보면 "감속이 명령(ctrl_avg)에서 시작됐는지, 명령은 일정한데 실제
  속도만 처졌는지"를 구분할 수 있음 — 후자라면 확실히 하드웨어/노면 쪽 원인.

**2026-08-17 23:42 speed=20 run(2회차)에서 결정적 단서 확보:** segment5가 dt=1.133s로
이웃 구간(0.65~0.89s)의 거의 2배로 길어지면서 real_mps만 0.927로 급감(이웃은 1.3~1.6).
그런데 **그 구간의 `ctrl_avg`(20.00)도 `vesc_avg`(1.599)도 이웃 구간들과 사실상 동일** —
즉 모터 커맨드도 안 변했고 모터 축 회전수 기반 추정치(VESC)도 안 떨어졌는데 지상 실측
속도만 뚝 떨어짐. VESC는 "모터가 얼마나 돌았는지"를 재는 것이지 "차가 얼마나 나갔는지"를
재는 게 아니므로, 이 패턴(모터 회전은 유지·지상속도만 하락)은 **바퀴가 순간적으로 헛돌며
견인력을 잃었다가(traction loss/slip) 다시 노면을 붙잡는 현상**과 정확히 일치함. 배터리
전압 드롭이나 VESC 전류제한이었다면 모터 회전수(=vesc_avg) 자체가 같이 떨어졌어야 하는데
그러지 않았으므로, **원인 후보를 "노면-타이어 그립 부족(고속 직진 시 순간 슬립)" 쪽으로
좁혀도 될 것 같음** — 다음 조사 시 타이어 상태/노면 재질/공기압 확인 우선순위로.

**2026-08-17 23:45 speed=20 run(4회차)에서 위치 상관관계 추가 확보:** 이 run에서도 다시
segment5(0m 출발선 기준 누적거리 5×1.05m≈5.25m 지점, dt=0.864s로 이웃보다 김, real=1.215로
급감)에서 같은 패턴 재발. **23:42 run과 23:45 run 둘 다 정확히 5번째 마크(≈5.25m 지점)에서
발생** — 두 run 모두 0m 출발선에서 새로 시작했으므로 이건 우연이 아니라 **트랙 위 약
5m 지점에 고정된 물리적 원인(이음새/오염/미끄러운 패치 등)이 있을 가능성이 높다는 뜻**.
다음 세션에서 그 지점을 육안으로 확인해볼 것 — 시간 기반(모터/전류) 가설보다 이쪽이
훨씬 유력해짐.

**속도=5에서 이미 나온 시사점** (아직 결론 아님, 더 많은 speed 필요):
- 기존 `METERS_PER_SPEED_UNIT=0.1347`(speed 5/10 2점 회귀)로 예측한 값(0.673 m/s)이 실측
  (0.431 m/s)보다 56% 높음 — 즉 저속 구간(≈5)에서 기존 회귀직선이 실제보다 위에 있음.
  이는 데드존 x절편 추정(speed≈1.4)이 실제보다 낮게(더 왼쪽으로) 잡혀 있었을 가능성 시사 —
  speed=10 실측이 들어오면 새 2점(5,10)만으로도 데드존이 얼마나 이동하는지 바로 확인 가능.
- VESC(`v_mps`)는 실제보다 평균 7.4% 낮게 측정 — 이 정도면 이후 speed들에서도 비슷한
  방향/크기로 나오는지 볼 것. 방향이 일관되면(항상 실제보다 낮음) 시스템적 오차(예: 타이어
  반경 가정값 오류, ERPM gain 오차)일 가능성 — 랜덤 잡음이 아니라 보정 가능한 오차일 수 있음.
- 개별 구간 dt가 전부 1초 미만이라 개별구간 표보다 회귀값(0.431)을 최종치로 채택.

## 5. `measure_speed_calibration.py` 전체 코드 (백업)

경로: `xycar_ws/src/track_drive/track_drive/measure_speed_calibration.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
#=============================================
# measure_speed_calibration.py — "찍히는 speed"(커맨드 단위 ctrl_speed / VESC 실측
# v_mps) vs 줄자+시간차로 잰 실제 지상속도(m/s) 비교 도구.
#
# ── 왜 필요한가 ──
# README §6.5의 METERS_PER_SPEED_UNIT(=0.1347)은 speed=5/10 단 2점 실측(각 2회 주행)만으로
# 낸 회귀값이라 "2점 외삽 한계"로 이미 명시돼 있고(x절편 speed≈1.4를 데드존으로 추정한 것도
# 실측이 아니라 추정치), VESC 실측(v_mps, README §7)도 지금까지 지상 실측(줄자)으로
# 교차검증된 적이 없다. 이 도구는 그 둘을 동시에, 여러 속도 지점에서 검증하기 위한 것.
#
# ── 원리 (사용자 계획 그대로) ──
# 트랙에 줄자로 등간격 지점을 표시해두고, 차량이 각 지점을 지날 때마다 Enter를 눌러
# 타임스탬프를 찍는다. 구간(mark[i-1]→mark[i])의 실제속도 = 구간거리 / 구간시간(뺄셈).
# 첫 구간(출발→mark1)은 초반가속이 섞여 있어 "정속" 가정이 깨지므로 참고용으로만 보여주고
# steady-state 평균/비교에서는 제외한다 — 이게 "초반가속을 고려한 뺄셈 연산"의 실제 구현.
# 그 구간 시간창 동안 로깅해둔 ctrl_speed/v_mps 평균과 실제속도를 나란히 비교해 오차%를 낸다.
#
# ── 측정 오차에 대한 주의 ──
# 사람이 Enter를 누르는 반응시간 오차(대략 0.1~0.2초)가 있다. 구간 시간이 1초 미만이면
# 이 오차가 전체의 10%+ 를 차지할 수 있다 — 기본 간격을 40cm로 좁게 잡은 만큼(예상 dt가
# 1초를 한참 밑돎) **개별 구간 숫자 하나하나는 잡음이 크다고 보고, 아래 두 가지 보정을
# 반드시 같이 볼 것**:
#   ① 가속구간 자동감지 — "첫 구간만 가속"이라고 가정하지 않는다. 연속 2개 구간의
#      실제속도가 서로 12% 이내로 비슷해지는 지점부터 steady로 판단한다(40cm 간격이면
#      가속이 여러 구간에 걸쳐 있을 수 있어서).
#   ② 회귀 기반 steady 속도 — steady로 판단된 구간의 각 Enter 시점(t, 누적거리)들을
#      최소자승 회귀(기울기=속도)로 한 번에 묶어서 낸 값. 개별 Enter 하나의 반응시간
#      잡음이 상쇄되므로, 구간별 표보다 이 값을 최종 수치로 더 신뢰할 것.
#
# ── 두 가지 모드 ──
# 1) 패시브 모드(기본, --drive-speed 생략): 이미 떠 있는 다른 노드(track_drive.py 등)가
#    xycar_motor를 발행 중일 때 그 값만 구독해서 관찰. 실제 주행(차선추종 등)의 속도
#    프로파일을 검증할 때 사용.
# 2) 능동 모드(--drive-speed X): 이 도구가 직접 angle=0, speed=X를 xycar_motor로 발행해
#    "직진 정속 테스트"를 만든다(가속 램프는 config.py SPEED_ACCEL_STEP과 동일하게 재현).
#
#    ★★★ 안전 — 능동 모드는 차선/장애물 인식이 전혀 없는 완전 개루프 주행이다 ★★★
#      - track_drive.py(또는 xycar_motor를 발행하는 다른 노드)는 반드시 꺼둘 것
#        (같은 토픽에 두 발행자가 붙으면 명령이 뒤섞인다)
#      - 사람이 없는 직선 트랙에서만, 즉시 멈출 수 있게 준비할 것(Ctrl+C 또는 리모컨
#        킬스위치) — Ctrl+C를 누르면 즉시 speed=0을 발행하고 종료한다
#      - --max-duration(기본 10초)이 지나면 마크 입력과 무관하게 자동으로 speed=0 발행
#
# ── 사용법(능동 모드 예시) ──
#   python3 measure_speed_calibration.py --drive-speed 15
#   (--interval-m 기본값 0.4 그대로 사용 — 줄자로 40cm 간격 표시를 미리 해둔다)
#   1) 차량을 정지선 뒤에 두고, 그 지점부터 줄자로 40cm 간격 표시를 미리 해둔다.
#   2) 스크립트 실행 → "0m 지점(출발) — 준비되면 Enter" 프롬프트에서 Enter →
#      그 즉시 차량이 speed=15 목표로 출발(가속램프 적용)하고 mark0(0m)가 기록된다.
#   3) 차량이 각 표시 지점(0.4m, 0.8m, 1.2m, ...)을 지나는 순간마다 Enter.
#   4) 다 지났으면 'q' + Enter로 종료 → 자동 speed=0 발행 + 구간별 결과표 출력 +
#      CSV 저장. 터미널에 출력된 표를 그대로 복사해 Claude에게 붙여넣으면 취합/판단.
#
# ── 사용법(패시브 모드) ──
#   python3 measure_speed_calibration.py
#   (track_drive.py를 정상 실행해둔 상태에서, 직선 구간을 지날 때 마크 Enter만 누르면 됨 —
#    첫 Enter가 0m 기준점이라, 반드시 차량이 표시해둔 0m 지점을 지나는 순간 눌러야 한다)
#
# ── config.py와 동기화 유지 ──
# 아래 상수들은 config.py에서 그대로 복사해 왔다(measure_lidar_camera_offset.py와 같은
# 관례 — 이 도구는 track_drive 패키지를 import하지 않고 독립 실행됨). config.py에서 값이
# 바뀌면 여기도 같이 갱신할 것.
#=============================================
import argparse
import csv
import datetime
import os
import statistics
import threading
import time

import rclpy
from rclpy.node import Node
from rclpy.qos import qos_profile_sensor_data
from std_msgs.msg import Float32, Float32MultiArray

# ── config.py와 반드시 동기화 유지 ──
METERS_PER_SPEED_UNIT = 0.1347     # README §6.5, speed 5~10 2점 회귀(현재값, 비교 기준선)
VESC_SPEED_TO_ERPM_GAIN = 4614.0   # vesc.yaml speed_to_erpm_gain 실측값
SPEED_ACCEL_STEP = 0.4             # 가속 램프(주기당 최대 증가량) — control_loop과 동일 주기 가정
CONTROL_PERIOD_SEC = 0.05          # track_drive.py control_loop 주기(20Hz)와 동일


class SpeedCalibNode(Node):
    def __init__(self, motor_topic, vesc_topic, drive_speed, max_duration):
        super().__init__('measure_speed_calibration')
        self.drive_speed = drive_speed
        self.max_duration = max_duration

        self._lock = threading.Lock()
        self.ctrl_samples = []   # [(t, ctrl_speed), ...] — xycar_motor에서 관측
        self.vesc_samples = []   # [(t, v_mps), ...]

        self.create_subscription(Float32MultiArray, motor_topic, self._cb_motor, 10)
        self.create_subscription(Float32, vesc_topic, self._cb_vesc, qos_profile_sensor_data)

        self._motor_pub = None
        self._drive_t0 = None
        self._cur_speed = 0.0
        self._force_stopped = False
        if drive_speed is not None:
            self._motor_pub = self.create_publisher(Float32MultiArray, motor_topic, 10)
            self.create_timer(CONTROL_PERIOD_SEC, self._drive_step)

    def _cb_motor(self, msg):
        if len(msg.data) < 2:
            return
        with self._lock:
            self.ctrl_samples.append((time.time(), float(msg.data[1])))

    def _cb_vesc(self, msg):
        with self._lock:
            self.vesc_samples.append((time.time(), float(msg.data) / VESC_SPEED_TO_ERPM_GAIN))

    def start_drive(self, t0):
        self._drive_t0 = t0

    def _drive_step(self):
        if self._drive_t0 is None or self._motor_pub is None:
            return
        elapsed = time.time() - self._drive_t0
        if elapsed >= self.max_duration:
            if not self._force_stopped:
                self._force_stopped = True
                self.get_logger().warn(
                    f'[안전정지] --max-duration({self.max_duration:.1f}s) 경과 — speed=0 강제 발행')
            self._cur_speed = 0.0
        else:
            target = self.drive_speed
            step = SPEED_ACCEL_STEP
            if self._cur_speed < target:
                self._cur_speed = min(target, self._cur_speed + step)
            elif self._cur_speed > target:
                self._cur_speed = max(target, self._cur_speed - step)
        self._publish(0.0, self._cur_speed)

    def _publish(self, angle, speed):
        msg = Float32MultiArray()
        msg.data = [float(angle), float(max(-100.0, min(100.0, speed)))]
        self._motor_pub.publish(msg)

    def stop_motor(self):
        if self._motor_pub is not None:
            self._publish(0.0, 0.0)

    def samples_snapshot(self):
        with self._lock:
            return list(self.ctrl_samples), list(self.vesc_samples)


def _input_worker(node, interval_m, markers, stop_event):
    """마크 입력 전담 스레드. 첫 Enter가 0m 기준점(+능동모드면 출발 트리거)이고,
    이후 매 Enter가 다음 마크. 숫자를 입력하면 그 마크의 절대거리(m)를 균일간격 대신
    직접 지정(간격이 고르지 않은 트랙 사정 대응용), 'q'는 종료."""
    if node.drive_speed is not None:
        print(f'\n[능동모드] 0m 지점(출발선)에 차량을 두고 준비되면 Enter — '
              f'즉시 speed={node.drive_speed:.1f} 목표로 출발합니다.')
    else:
        print('\n[패시브모드] 차량이 0m 기준점을 지나는 순간 Enter.')
    try:
        input()
    except EOFError:
        stop_event.set()
        return
    t0 = time.time()
    markers.append({'idx': 0, 't': t0, 'dist_m': 0.0})
    if node.drive_speed is not None:
        node.start_drive(t0)

    idx = 1
    while not stop_event.is_set():
        try:
            line = input(f'  mark{idx} ({idx * interval_m:.1f}m 예상, 숫자=실측거리 직접입력, '
                          f'q=종료): ').strip()
        except EOFError:
            break
        if line.lower() == 'q':
            break
        dist = idx * interval_m
        if line:
            try:
                dist = float(line)
            except ValueError:
                print('    (숫자로 해석 안 됨 — 균일 간격으로 처리)')
        markers.append({'idx': idx, 't': time.time(), 'dist_m': dist})
        idx += 1
    stop_event.set()


def _mean(vals):
    return statistics.fmean(vals) if vals else None


def _detect_steady_start(rows, rel_tol=0.12, consec=2):
    """rows(순서대로)에서 steady-state가 시작되는 인덱스(0-based)를 찾는다.
    "첫 구간만 가속"이라고 하드코딩하지 않는 이유: --interval-m을 40cm처럼 좁게 잡으면
    가속이 한 구간 안에서 안 끝나고 여러 구간에 걸칠 수 있다. 연속 consec개 구간의
    실제속도가 서로 rel_tol(기본 12%) 이내로 수렴하는 첫 지점을 steady 시작으로 본다 —
    휴리스틱이라 완벽하진 않으니, 출력되는 구간별 표를 보고 이상하면 직접 판단할 것."""
    if len(rows) < consec:
        return len(rows)
    for i in range(len(rows) - consec + 1):
        vals = [r['real_mps'] for r in rows[i:i + consec]]
        vmean = statistics.fmean(vals)
        if vmean > 0 and (max(vals) - min(vals)) <= rel_tol * vmean:
            return i
    return len(rows)


def _regression_speed(marks):
    """steady 구간에 속한 마크들(t, dist_m)의 최소자승 회귀 기울기 = 속도(m/s).
    개별 Enter 하나의 반응시간 잡음이 여러 점에 걸쳐 상쇄되므로, 구간(2점)만으로 낸
    real_mps보다 잡음에 강건하다 — 40cm처럼 좁은 간격에서 특히 유용."""
    if len(marks) < 3:
        return None
    ts = [m['t'] for m in marks]
    ds = [m['dist_m'] for m in marks]
    tbar, dbar = _mean(ts), _mean(ds)
    den = sum((t - tbar) ** 2 for t in ts)
    if den <= 0:
        return None
    return sum((t - tbar) * (d - dbar) for t, d in zip(ts, ds)) / den


def _analyze(markers, ctrl_samples, vesc_samples, out_dir, drive_speed):
    if len(markers) < 2:
        print('\n마크가 2개 미만이라 분석할 구간이 없습니다.')
        return

    rows = []
    for i in range(1, len(markers)):
        m0, m1 = markers[i - 1], markers[i]
        dt = m1['t'] - m0['t']
        dd = m1['dist_m'] - m0['dist_m']
        if dt <= 0 or dd <= 0:
            continue
        real_mps = dd / dt
        ctrl_win = [s for (t, s) in ctrl_samples if m0['t'] <= t <= m1['t']]
        vesc_win = [s for (t, s) in vesc_samples if m0['t'] <= t <= m1['t']]
        ctrl_avg = _mean(ctrl_win)
        vesc_avg = _mean(vesc_win)
        pred_mps = ctrl_avg * METERS_PER_SPEED_UNIT if ctrl_avg is not None else None
        implied_unit = (real_mps / ctrl_avg) if ctrl_avg else None
        rows.append(dict(seg=i, dt=dt, dd=dd, real_mps=real_mps,
                          ctrl_avg=ctrl_avg, vesc_avg=vesc_avg, pred_mps=pred_mps,
                          implied_unit=implied_unit, n_ctrl=len(ctrl_win), n_vesc=len(vesc_win)))

    steady_start_idx = _detect_steady_start(rows)
    for j, r in enumerate(rows):
        r['accel_phase'] = j < steady_start_idx
    if steady_start_idx >= len(rows):
        print('\n⚠ steady 구간을 자동감지하지 못했습니다(끝까지 가속/변속 중으로 보임) — '
              '마크를 더 찍거나(더 먼 거리) 아래 표를 보고 직접 steady 구간을 판단하세요.')
    else:
        print(f'\nsteady 시작 자동감지: segment {steady_start_idx + 1}부터 '
              f'(연속 2구간 실제속도가 12% 이내로 수렴하는 지점)')

    print('\n=== 구간별 결과 ===')
    header = (f'{"seg":>4} {"구간(m)":>8} {"dt(s)":>7} {"실제(m/s)":>10} '
              f'{"ctrl_avg":>9} {"pred(cfg)":>10} {"vesc_avg":>9} {"단위당(m/s)":>11}')
    print(header)
    for r in rows:
        tag = '가속구간*' if r['accel_phase'] else ''
        print(f'{r["seg"]:>4} {r["dd"]:>8.2f} {r["dt"]:>7.3f} {r["real_mps"]:>10.3f} '
              f'{r["ctrl_avg"] if r["ctrl_avg"] is not None else float("nan"):>9.2f} '
              f'{r["pred_mps"] if r["pred_mps"] is not None else float("nan"):>10.3f} '
              f'{r["vesc_avg"] if r["vesc_avg"] is not None else float("nan"):>9.3f} '
              f'{r["implied_unit"] if r["implied_unit"] is not None else float("nan"):>11.4f} {tag}')
        if r['dt'] < 1.0:
            print(f'      ⚠ 구간 시간 {r["dt"]:.2f}s < 1초 — 사람 반응시간 오차(~0.1~0.2s)가 '
                  f'{r["dt"]:.2f}s의 10%+ 를 차지할 수 있습니다. 간격을 넓히거나 반복측정 권장.')
        if r['n_ctrl'] == 0:
            print('      ⚠ 이 구간엔 ctrl_speed 샘플이 없습니다(토픽 미수신?) — ctrl_avg 신뢰 불가.')
        if r['n_vesc'] == 0:
            print('      ⚠ 이 구간엔 VESC 샘플이 없습니다(vesc_speed_bridge 미실행/브리지 끊김?).')

    steady = [r for r in rows if not r['accel_phase']]
    print('\n=== steady-state 요약(자동감지된 가속구간 제외) ===')
    if steady:
        real_vals = [r['real_mps'] for r in steady]
        ctrl_vals = [r['ctrl_avg'] for r in steady if r['ctrl_avg'] is not None]
        vesc_vals = [(r['real_mps'], r['vesc_avg']) for r in steady if r['vesc_avg'] is not None]
        unit_vals = [r['implied_unit'] for r in steady if r['implied_unit'] is not None]
        print(f'steady 구간 수: {len(steady)}')
        print(f'실제속도 평균={_mean(real_vals):.3f} m/s '
              f'(표준편차={statistics.pstdev(real_vals):.3f}, 편차가 크면 아직도 가속/감속 중이란 뜻)')
        if ctrl_vals:
            print(f'ctrl_speed 평균={_mean(ctrl_vals):.2f}')
        if unit_vals:
            implied = _mean(unit_vals)
            cfg_err_pct = abs(implied - METERS_PER_SPEED_UNIT) / METERS_PER_SPEED_UNIT * 100
            print(f'실측 기반 METERS_PER_SPEED_UNIT ≈ {implied:.4f} '
                  f'(현재 config.py 값 {METERS_PER_SPEED_UNIT} 대비 오차 {cfg_err_pct:.1f}%)')
        start_pos = rows[steady_start_idx]['seg'] - 1 if steady_start_idx < len(rows) else None
        if start_pos is not None:
            reg_speed = _regression_speed(markers[start_pos:])
            if reg_speed is not None:
                print(f'회귀 기반 steady 실제속도(Enter 반응시간 잡음에 강건) = {reg_speed:.3f} m/s '
                      f'(마크 {len(markers) - start_pos}개 사용) ← 40cm처럼 좁은 간격이면 이 값을 '
                      f'구간별 표보다 우선 신뢰할 것')
                if ctrl_vals:
                    implied_reg = reg_speed / _mean(ctrl_vals)
                    print(f'  → 회귀 기반 METERS_PER_SPEED_UNIT ≈ {implied_reg:.4f}')
        if vesc_vals:
            errs = [abs(v - r) / r * 100 for r, v in vesc_vals if r > 0]
            if errs:
                print(f'VESC(v_mps) vs 실제 평균 오차 = {_mean(errs):.1f}% (n={len(errs)})')
    else:
        print('전 구간이 가속구간으로 판단돼 steady 구간이 없습니다 — 마크를 더 찍어보세요.')

    os.makedirs(out_dir, exist_ok=True)
    ts = datetime.datetime.now().strftime('%Y%m%d_%H%M%S')
    tag = f'drive{drive_speed:g}' if drive_speed is not None else 'passive'
    csv_path = os.path.join(out_dir, f'speed_calib_{tag}_{ts}.csv')
    with open(csv_path, 'w', newline='') as f:
        w = csv.writer(f)
        w.writerow(['seg', 'accel_phase', 'segment_m', 'dt_s', 'real_mps', 'ctrl_avg',
                     'pred_mps_cfg', 'vesc_avg_mps', 'implied_meters_per_unit', 'n_ctrl', 'n_vesc'])
        for r in rows:
            w.writerow([r['seg'], r['accel_phase'], r['dd'], r['dt'], r['real_mps'],
                        r['ctrl_avg'], r['pred_mps'], r['vesc_avg'], r['implied_unit'],
                        r['n_ctrl'], r['n_vesc']])
    print(f'\nCSV 저장: {csv_path}')
    print('(위 "구간별 결과"/"steady-state 요약" 콘솔 출력을 그대로 복사해 Claude에게 붙여넣으면 됩니다.)')


def main():
    ap = argparse.ArgumentParser(description=__doc__,
                                  formatter_class=argparse.RawDescriptionHelpFormatter)
    ap.add_argument('--interval-m', type=float, default=0.4,
                     help='마크 간 기본 간격(m), 균일 간격 가정 시 사용(기본 0.4=40cm — '
                          '짧을수록 개별 구간은 잡음이 커지니 steady 구간은 회귀값을 볼 것)')
    ap.add_argument('--drive-speed', type=float, default=None,
                     help='능동모드: 이 값으로 angle=0 직진 정속 명령을 직접 발행. '
                          '생략하면 패시브(관찰만) 모드.')
    ap.add_argument('--max-duration', type=float, default=10.0,
                     help='능동모드 안전 컷오프(초) — 경과 시 강제로 speed=0 (기본 10.0)')
    ap.add_argument('--motor-topic', default='xycar_motor')
    ap.add_argument('--vesc-topic', default='/vesc_speed_erpm')
    ap.add_argument('--out-dir', default=os.path.join(os.path.dirname(os.path.abspath(__file__)),
                                                        'speed_calib_logs'))
    args = ap.parse_args()

    if args.drive_speed is not None:
        print('=' * 60)
        print('★★★ 능동모드 — 완전 개루프 직진 주행 ★★★')
        print('- track_drive.py 등 xycar_motor를 발행하는 다른 노드가 꺼져 있는지 확인하세요.')
        print('- 사람이 없는 직선 트랙에서만 실행하고, Ctrl+C로 언제든 즉시 정지시킬 수 있습니다.')
        print(f'- {args.max_duration:.1f}초가 지나면 자동으로 speed=0이 발행됩니다.')
        print('=' * 60)

    rclpy.init(args=[])
    node = SpeedCalibNode(args.motor_topic, args.vesc_topic, args.drive_speed, args.max_duration)

    markers = []
    stop_event = threading.Event()
    th = threading.Thread(target=_input_worker, args=(node, args.interval_m, markers, stop_event),
                           daemon=True)
    th.start()

    try:
        while not stop_event.is_set():
            rclpy.spin_once(node, timeout_sec=0.05)
    except KeyboardInterrupt:
        print('\n[Ctrl+C] 즉시 정지합니다.')
    finally:
        node.stop_motor()
        for _ in range(3):   # 정지 명령이 확실히 나가도록 몇 차례 더 publish
            rclpy.spin_once(node, timeout_sec=0.05)
        ctrl_samples, vesc_samples = node.samples_snapshot()
        node.destroy_node()
        rclpy.shutdown()

    _analyze(markers, ctrl_samples, vesc_samples, args.out_dir, args.drive_speed)


if __name__ == '__main__':
    main()
```
