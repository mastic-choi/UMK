# 새로운 조향테스트

Pure Pursuit + 코너감속 파라미터를 `pp_tune_gridsearch.py`(화이트박스 합성 시뮬레이션)로
재튜닝하는 작업의 진행 기록. **실차 미검증 — 진행 중인 작업 로그**이며, README.md의 정식
버전 히스토리(§0.5.x)와 달리 아직 config.py에 아무것도 반영되지 않은 상태의 원자재 데이터를
모아두는 용도다. 실측속도 확보 + 최종 값 결정 후 README §0.5.x로 정리 이관 예정.

관련 코드: [pp_tune_gridsearch.py](pp_tune_gridsearch.py), [README.md §0.5.14](README.md)

---

## 0. 이번에 시뮬레이터 자체에 생긴 변화 (2026-08-17)

기존 `pp_tune_gridsearch.py`는 직진/90도커브(가정 반경 1.2m)/S자커브(가정 반경 1.3m) 3개를
독립적으로만 채점했다. 이번 세션에서:

1. **실전트랙 시나리오 추가** — 사용자가 제공한 실제 트랙 도면 실측값(좌상단/좌하단 코너
   R1,950mm 동일, 우측 지그재그 구간 R1,615/1,530/1,595/1,470mm 4원호)으로 직진→커브→직진→
   지그재그→직진→커브→직진이 이어지는 연속 경로를 추가. 모드별(long_straight/curve/zigzag)
   cte뿐 아니라 **모드 전환구간**(코너·지그재그 진입/이탈 순간) cte를 따로 집계해 가중치를
   더 준다 — 전환 중에만 유독 튀는 조합을 잡아내기 위함.
2. **`PATH_EMA_ALPHA`(경로 스무딩) 그리드서치 포함** — 실제 `perception/lane_util.py.
   _update_path()`가 매 프레임 새로 피팅된 경로를 직전 프레임 경로와 EMA 블렌딩하는 로직을
   시뮬레이터도 동일하게 재현하도록 추가. 파라미터 총 **22개 → 23개**.
3. **폐루프 앨리어싱 버그 2건 수정** — 실전트랙이 닫힌 루프라 경로상 멀리 떨어진 두 구간이
   월드좌표상 가까워질 수 있는데, `_nearest_point()`(채점용 최근접점)와 `_local_path_px()`
   (센서 웨이포인트 합성)가 둘 다 전역 탐색을 쓰고 있어서 차량이 살짝만 벗어나도 엉뚱한
   구간의 점이 섞여 들어가 조향이 즉시 폭주하는 버그가 있었다. 직전 틱 위치 기준 탐색창
   (`NEAREST_WINDOW_M`)으로 고쳤다. (자세한 내용은 README §0.5.14)
4. **`--optuna` 모드 추가** — 기존 랜덤서치(`--samples`) 대신 Optuna TPE(베이지안 최적화)로
   같은 23차원 공간을 탐색할 수 있게 `run_optuna_search()`/`--optuna --trials N --seed-file
   <json>` 추가. `--seed-file`로 이전 탐색이 찾은 best_params들을 `study.enqueue_trial()`로
   먼저 평가시켜, 새 탐색이 이전 결과를 버리지 않고 그 주변부터 이어서 찾게 한다.
5. **디바운스 파라미터(`DL_STABLE_FRAME_MIN`/`DL_STABLE_JUMP_MAX`) 추가 — 23개 → 25개** —
   "지금까지 설정한 파라미터 중 빼먹은 게 있는지" 사용자 요청으로 실제 코드를 대조하다가
   발견한 진짜 누락. `perception/lane_util.py._debounce()`가 코너 프리뷰 신호(lane_lookahead)
   를 확정하기 전에 거치는 N프레임 연속확인 게이트인데(`PATH_EMA_ALPHA`와는 별개의 스무딩
   단계), 시뮬레이터는 이 게이트 없이 고정 EMA로만 근사하고 있었다. `simulate()`에
   `_debounce()`와 동일한 confirmed/pending/pending_count 상태를 추가해 재현. 이전 라운드
   시드 파일(23키)은 자동으로 두 값의 config.py 현재값(2, 20.0)을 채워서 25차원 시드로
   확장되도록 방어 코드도 추가(`run_optuna_search()`가 `sp.get(k, BASELINE[k])`로 처리) —
   시드 파일을 손으로 갱신할 필요 없음.

---

## 1. 진행 기록

### 1차 — 랜덤서치 20,000샘플 (2026-08-17)

- **명령**: `pp_tune_gridsearch.py --samples 20000 --speeds {10,15,20} --seed 0` (환경엔
  numpy가 없어 스크래치 venv에 설치해 실행, 속도별 병렬 백그라운드)
- **소요**: 속도당 약 8분

| 속도 | baseline score | best score (20,000샘플) |
|---|---|---|
| 10 | 49.29 | 9.80 |
| 15 | 54.63 | 9.13 |
| 20 | 53.42 | 10.31 |

<details>
<summary>1차 best_params 전체 (23개 × 3속도)</summary>

| 파라미터 | speed10 | speed15 | speed20 |
|---|---|---|---|
| lookahead_base_px | 88.31 | 77.61 | 77.4 |
| lookahead_speed_gain | 1.936 | 1.52 | 2.293 |
| lookahead_max_px | 262.8 | 242.7 | 215.3 |
| wheelbase_px | 49.82 | 47.54 | 43.47 |
| alpha | 0.9439 | 0.3473 | 0.9349 |
| min_lookahead_px | 87.74 | 67.19 | 50.2 |
| dx_deadzone_px | 2.67 | 12.39 | 1.995 |
| lookahead_curvature_gain | 347.6 | 237.4 | 482.0 |
| lookahead_min_px | 55.92 | 32.01 | 55.49 |
| straight_curvature_eps | 0.00613 | 0.008626 | 0.002059 |
| straight_confirm_frames | 3 | 10 | 2 |
| straight_deadzone_px | 2.874 | 2.871 | 10.32 |
| straight_alpha | 0.8198 | 0.6489 | 0.9409 |
| straight_bias_ema_alpha | 0.3694 | 0.3602 | 0.5949 |
| speed_corner_min | 12.32 | 6.713 | 11.63 |
| corner_sign_ema_alpha | 0.746 | 0.7592 | 0.6604 |
| lane_lookahead_ref | 155.2 | 485.7 | 463.3 |
| speed_accel_step | 1.295 | 0.8852 | 1.098 |
| corner_hold_decay_lo | 0.8355 | 0.948 | 0.8041 |
| corner_hold_decay_hi | 0.9209 | 0.9827 | 0.9489 |
| corner_min_radius_px | 367.3 | 239.5 | 919.7 |
| corner_min_speed_scale | 0.245 | 0.1372 | 0.267 |
| path_ema_alpha | 0.5853 | 0.7507 | 0.6753 |

</details>

파라미터가 속도별로 꽤 흩어짐 — 스크립트 자체 주석대로 23차원 랜덤서치가 "동점에 가까운
국소최적점이 많아 고분산"이기 때문으로 판단.

### 2차 — Optuna TPE 10,000트라이얼 (2026-08-17, 1차 결과를 시드로 이어감)

- **명령**: `--optuna --trials 10000 --speeds {10,15,20} --seed 0 --seed-file <1차 best_params 3개>`
- **소요**: 속도당 약 6~7분

| 속도 | 1차(랜덤서치) best | 2차(Optuna) best | 개선폭 |
|---|---|---|---|
| 10 | 9.80 | **6.59** | -33% |
| 15 | 9.13 | **6.95** | -24% |
| 20 | 10.31 | **6.99** | -32% |

<details>
<summary>2차 best_params 전체 (23개 × 3속도)</summary>

| 파라미터 | speed10 | speed15 | speed20 |
|---|---|---|---|
| lookahead_base_px | 80.68 | 80.41 | 80.86 |
| lookahead_speed_gain | 1.235 | 1.354 | 1.341 |
| lookahead_max_px | 182.5 | 261.9 | 206.9 |
| wheelbase_px | 49.35 | 49.33 | 48.47 |
| alpha | 0.9167 | 0.8782 | 0.9162 |
| min_lookahead_px | 85.08 | 88.33 | 54.16 |
| dx_deadzone_px | 1.833 | 2.288 | 1.857 |
| lookahead_curvature_gain | 423.3 | 520.7 | 513.8 |
| lookahead_min_px | 46.21 | 49.46 | 46.56 |
| straight_curvature_eps | 0.004512 | 0.004514 | 0.001231 |
| straight_confirm_frames | 2 | 7 | 10 |
| straight_deadzone_px | 11.24 | 3.865 | 7.021 |
| straight_alpha | 0.392 | 0.7543 | 0.8595 |
| straight_bias_ema_alpha | 0.7216 | 0.7517 | 0.7664 |
| speed_corner_min | 14.84 | 14.79 | 8.258 |
| corner_sign_ema_alpha | 0.9859 | 0.8279 | 0.2381 |
| lane_lookahead_ref | 509.3 | 537.3 | 210.5 |
| speed_accel_step | 1.524 | 1.394 | 0.9932 |
| corner_hold_decay_lo | 0.8277 | 0.8566 | 0.846 |
| corner_hold_decay_hi | 0.9355 | 0.925 | 0.9336 |
| corner_min_radius_px | 419.7 | 851.0 | 196.1 |
| corner_min_speed_scale | 0.2613 | 0.07954 | 0.1983 |
| path_ema_alpha | 0.788 | 0.7209 | 0.7565 |

</details>

**세 속도가 거의 같은 값으로 수렴한 파라미터** (조향의 핵심, 속도보다 트랙 곡률/노이즈
특성에 더 좌우되는 것으로 보임):

| 파라미터 | 현재 config.py | speed10 | speed15 | speed20 |
|---|---|---|---|---|
| lookahead_base_px | 110.0 | 80.68 | 80.41 | 80.86 |
| wheelbase_px | 25.0 | 49.35 | 49.33 | 48.47 |
| alpha | 0.4842 | 0.917 | 0.878 | 0.916 |
| dx_deadzone_px | 4.277 | 1.833 | 2.288 | 1.857 |
| path_ema_alpha | 0.25 | 0.788 | 0.721 | 0.757 |

**여전히 속도별로 흩어지는 파라미터** (주로 코너 감속 관련, speed20에서 특히 크게 벌어짐 —
speed20은 `METERS_PER_SPEED_UNIT` 회귀범위(5~10) 밖 외삽이라 표시되는 속도라 노이즈일 수도
있음): `corner_sign_ema_alpha`(0.99/0.83/**0.24**), `lane_lookahead_ref`(509/537/**211**),
`corner_min_radius_px`(420/851/**196**), `speed_corner_min`(14.8/14.8/**8.3**).

### 3차 — Optuna 넓은 속도범위 (완료, 2026-08-17, 23차원 — 4차 디바운스 반영 전)

- **목적**: 실측속도가 아직 불가능(몇 시간 후 가능)한 동안, 실측속도가 어디로 나오든 근처
  값을 바로 쓸 수 있도록 속도 커버리지를 미리 넓혀둔다.
- **명령**: `--optuna --trials 8000 --speeds {5, 7.5} / {12.5, 17.5} / {22.5, 25} --seed 0
  --seed-file <1차+2차 best_params 6개 전부>` (2개 속도씩 묶어 3개 백그라운드 프로세스)

| 속도 | baseline score | best score (8,000 trials) |
|---|---|---|
| 5 | 49.29 | 6.60 |
| 7.5 | 49.29 | 6.32 |
| 12.5 | 50.45 | 6.54 |
| 17.5 | 55.37 | 7.18 |
| 22.5 | 59.12 | 6.93 |
| 25 | 56.36 | 6.72 |

**중요한 발견 — speed=5와 7.5는 사실상 speed=10과 완전히 같은 시나리오였다.** baseline
score(49.29)와 best_params가 speed=10의 2차 결과와 정확히 일치한다. 원인: `config.py`의
`SPEED_CORNER_MIN`은 파일 상단에 `7.0`으로 선언돼 있지만, 하단 `PP_TUNE_ACTIVE_PRESET`
프리셋 적용부가 `globals().update()`로 **10.0**을 덮어써서 실제 로드값은 10.0이다.
`_lane_drive()`의 `target_speed = max(SPEED_CORNER_MIN, speed_norm*(...))` 공식상
speed_norm이 10.0 미만이면 무조건 10.0으로 바닥이 잡혀 speed_norm 자체가 무의미해진다 —
계산이 잘못된 게 아니라 "SPEED_CORNER_MIN=10.0인 한 그 아래 속도들은 전부 speed=10과
동일하게 행동한다"는 걸 실측으로 확인한 것. 이후 재탐색(4차)에서 5/7.5는 별도로 다시
돌리지 않고 speed=10 결과를 그대로 대표값으로 쓴다.

### 4차 — 디바운스 파라미터 추가 + 25차원 재탐색 (완료, 2026-08-17)

- **계기**: 사용자가 "지금까지 설정한 조향 파라미터 중 빼먹은 게 있는지" 확인 요청 →
  코드 대조 중 `DL_STABLE_FRAME_MIN`/`DL_STABLE_JUMP_MAX`(코너 프리뷰 신호의 N프레임
  디바운스, §0.5 참고)가 진짜로 빠져있었던 것을 발견. 파라미터 **23개 → 25개**.
- **재계산 전략**: 새 차원이 추가되면 1~3차 결과는 "다른(더 단순한) 시뮬레이터 버전"에서
  나온 값이라 점수가 직접 비교 가능하지 않다 — 그렇다고 처음부터 다시 찾을 필요는 없고,
  1~3차가 찾은 best_params들을 새 2개 파라미터만 config.py 현재값(2, 20.0)으로 채운 뒤
  Optuna 시드로 그대로 재사용한다(`run_optuna_search()`의 `sp.get(k, BASELINE[k])` 방어
  코드로 자동 처리 — 시드 파일을 손으로 안 고쳐도 됨).
- **자원 제약**: 이 개발 PC가 성능코어 4개라 2단계로 나눠 진행:
  - **1단계(진행 중)**: 시드가 가장 풍부한 speed 10/15/20을 1차+2차 best_params 6개로
    시드해 `--optuna --trials 6000 --speeds 10 15 20 --seed 0 --seed-file <1차+2차 6개>`
    실행.
  - **2단계(진행 중, 3차 완료로 코어 확보)**: 3차가 끝나 speed=5/7.5는 speed=10과 동일
    시나리오임이 확인됐으므로(위 3차 "중요한 발견" 참고) 별도로 다시 안 돌리고, 실제로
    구분되는 4개 속도(12.5/17.5/22.5/25)만 1차+2차+3차 best_params 10개(5/7.5 제외
    중복 제거)를 전부 시드로 넣어 `--optuna --trials 8000 --speeds {12.5,17.5} /
    {22.5,25} --seed 0 --seed-file <10개>` 2개 프로세스로 실행.
- **상태**: 완료. speed=5/7.5는 speed=10과 동일 시나리오라 별도 실행 없이 그 값을 대표로 씀.

| 속도 | 3차 이하(23차원) best | 4차(25차원) best |
|---|---|---|
| 5 | 6.60 | (=10과 동일) |
| 7.5 | 6.32 | (=10과 동일) |
| 10 | 6.59 | **6.83** |
| 12.5 | 6.54 | **6.47** |
| 15 | 6.95 | **7.15** |
| 17.5 | 7.18 | **7.04** |
| 20 | 6.99 | **6.96** |
| 22.5 | 6.93 | **6.76** |
| 25 | 6.72 | **6.91** |

**결론 1 — 디바운스 파라미터를 추가해도 기존 핵심 결론이 뒤집히지 않았다.** 점수가 전부
6.4~7.2 사이로 23차원 때와 오차범위 안에서 비슷하다. 즉 빠져있던 파라미터가 최적해 자체를
크게 바꿀 만큼 중요하진 않았다 — 다만 이건 "몰라도 됐다"가 아니라 "이번 시뮬레이션 설정
안에서는 다른 23개가 이미 지배적이었다"는 뜻이라, §0에서 지적한 대로 여전히 그리드서치
대상에 넣어두는 게 맞다.

**결론 2 — `path_ema_alpha`가 9개 속도 전부에서 놀라울 정도로 좁게 수렴했다(0.748~0.800).**
지금까지 나온 것 중 가장 강한 신호다 — 속도와 사실상 무관하게 config.py 현재값(0.25)보다
3배 가까이 높은 값을 일관되게 원한다는 뜻. `lookahead_base_px`(77~84)/`wheelbase_px`
(45~50, speed=22.5만 45.16으로 소폭 예외)/`alpha`(대부분 0.82~0.95, speed=12.5만 0.75)도
비슷하게 좁은 범위로 수렴했다.

**결론 3 — 새로 추가한 `stable_frame_min`/`stable_jump_max`는 속도별로 흩어져 있고
(2~10 프레임, 10~58px) 점수에 미치는 영향도 상대적으로 작아 보인다.** 이 둘은 "빠뜨리면
안 되는 파라미터"이긴 했지만 "결과를 좌우하는 핵심 파라미터"는 아니었다는 뜻 — 코너감속
계열(`corner_sign_ema_alpha`/`lane_lookahead_ref`/`speed_corner_min`)도 여전히 흩어져
있는데, 그중 `corner_min_radius_px`만 865(10)→418(12.5)→567(15)→235(17.5)→217(20)→
197(22.5)→374(25)로 노이즈는 있지만 대체로 속도가 오를수록 낮아지는 경향이 보인다 —
게인 스케줄링(사용자가 물었던 "속도에 따라 파라미터를 가변화") 후보로 가장 유력해 보이나,
아직 진짜 추세인지 노이즈인지 `--sensitivity`로 재확인 필요.

---

## 2. 핵심 발견 / 주의사항

1. **`wheelbase_px`(25.0→~43~50)와 `path_ema_alpha`(0.25→~0.59~0.79)는 최근 커밋이 정확히
   반대 방향으로 낮췄던 값이다.**
   - `e86c023`: "speed15 프리셋 PP_WHEELBASE_PX 47.36→25.0 — 좌우 진동 원인"
   - `936dcd7`: "PATH_EMA_ALPHA 0.75 → 0.25(요청 반영)"
   시뮬레이션이 실제 트랙 곡률(실전트랙)을 처음 반영하면서 독립적으로 "그때 낮췄던 게
   과했다"는 방향의 결론에 세 번(1차 3속도) 다 도달했고, 2차(Optuna)에서 세 속도 모두
   거의 같은 값(~48~49 / ~0.72~0.79)으로 더 강하게 수렴했다. 방향이 일관될수록 흥미롭긴
   하지만, **정확히 실차 진동 문제로 낮췄던 값을 되돌리라는 결과라 실차 재검증 없이는
   절대 그대로 적용 금지.**
2. **시뮬레이션 자체의 한계**(스크립트 상단 주석 원문 그대로): 세그멘테이션 노이즈
   1.5px(가정), `DL_PIXELS_PER_METER=200px/m`(설계값), 지그재그 4원호의 회전각(도면에
   반지름만 있고 각도는 없어 `ZIGZAG_WIGGLE_DEG`로 임의 가정), VESC/IMU 미시뮬레이션(항상
   `_prev_speed` 폴백 경로만 검증), 모터/서보 반응 지연·배터리 전류 전혀 미모델링. 전부
   README §0.5.12/§0.5.14가 이미 명시한 한계와 동일한 종류.
3. **speed20(및 그 이상)은 `METERS_PER_SPEED_UNIT` 실측 회귀구간(speed 5~10) 밖 외삽**이라,
   이 구간에서 코너감속 파라미터가 유독 크게 흩어지는 게 진짜 속도 의존성인지 회귀 외삽
   오차인지 아직 구분 안 됨.
4. **config.py에는 지금까지 아무것도 반영 안 됨** — README §0.5.12/§0.5.13이 지켜온 절차
   (정식 실행 + 사용자 확인 후에만 반영)를 그대로 따름.

### 5차 — `METERS_PER_SPEED_UNIT` 실측 재보정(0.1347→0.0848) 반영 후 재탐색 + config.py 배포 (완료, 2026-08-18)

- **계기**: 팀원(이지유) 브랜치의 `실제속도측정.md`/`measure_speed_calibration.py` 실측
  데이터를 병합 — speed 5/10/15 재실측(각 3~4회) 결과 기존 2점 회귀(0.1347)가 실제보다
  약 58% 과대추정하고 있었음을 확인. 5/10/15 3점 원점고정 회귀로 `0.0848` 채택(20은
  견인력 손실로 별도 비선형 구간이라 이 상수 미적용 — config.py 주석 참고).
- **부수 조치**: `MAX_SIM_T`를 물리 속도 비율에 비례해 자동 스케일링(30.0*0.1347/신규값
  ≈ 47.65s)하도록 수식으로 변경 — 느려진 물리로 완주시간이 늘어난 만큼 안 그러면 부당한
  미완주 페널티 발생.
- **재탐색**: 4차 25차원 시드 7개로 7개 속도(10/12.5/15/17.5/20/22.5/25) 전부 Optuna
  재탐색. 첫 시도(8000트라이얼)는 TPE 샘플러 자체의 `log_pdf` 계산이 누적 트라이얼
  수에 비례해 느려지는 현상(py-spy로 스택 확인)으로 1시간 넘게 안 끝나 중단 — 2000
  트라이얼로 재시도해 정상 완료(격리 시 1속도당 약 2분 37초).

| 속도 | best score | `PP_WHEELBASE_PX` |
|---|---|---|
| 10 | 8.61 | 49.39 |
| 12.5 | 8.84 | 49.17 |
| 15 | 8.66(추가 2000트라이얼로 9.00→8.66 소폭 개선) | 49.97 |
| 17.5 | 8.38 | 50.00 |
| 20 | 8.14 | 49.36 |
| 22.5 | 7.98 | 49.29 |
| 25 | 7.80 | 49.99 |

(baseline이 40~47점대로 이전 라운드의 42~59점대보다 낮아진 것도 물리가 바뀐 영향 —
`score()`의 `elapsed_s` 항이 완주시간에 비례해서, cte 자체는 이전과 거의 동일함에도
전체 점수 스케일이 이동했다. 절대값이 아니라 같은 라운드 안에서의 상대 비교로만 볼 것.)

**핵심 확인 — `PP_WHEELBASE_PX`는 물리를 크게 고쳤는데도(0.1347→0.0848) 48.8~50.0으로
거의 그대로다.** 물리 보정 전(4차, 평균 48.6)과 사실상 동일한 결론이라 이 값의 신뢰도가
한층 올라갔다. 반면 `path_ema_alpha`는 물리 보정 후 오히려 더 흩어짐(0.47~0.80, 물리
보정 전엔 0.72~0.80으로 더 좁았음) — speed=10/12.5(0.47~0.50)와 15 이상(0.64~0.80)이
갈리는 패턴이 새로 보임, 진짜 추세인지는 미확인.

**config.py 배포 (2026-08-18)**: `PP_TUNE_PRESETS`를 이전 speed15/speed25(22개
파라미터, 구물리값) 2개에서 위 7개 속도 전부(25개 파라미터: PATH_EMA_ALPHA/
DL_STABLE_FRAME_MIN/DL_STABLE_JUMP_MAX 포함)로 전면 교체하고 `PP_TUNE_ACTIVE_PRESET
= 'speed15'`로 활성화(사용자 확인 후 진행). **`PP_WHEELBASE_PX`(49.97)는 과거 47.36→25.0
으로 되돌려졌던 진동 문제와, `SPEED_ACCEL_STEP`(1.476)/`SPEED_CORNER_MIN`(15.77)은
각각 과거 LVC 트립(1.014)/트랙이탈(14.05)로 되돌려졌던 값과 같은 구간으로 다시
수렴했다** — config.py §8 상단 주석에 상세 경고 남겨둠, 첫 실차 테스트 시 최우선
확인 대상. `ANGLE_RATE_MAX`(조향 변화율 제한)는 아직 시뮬레이터에 반영 전 상태에서
나온 값이라는 것도 감안할 것.

---

## 3. 다음 단계

- [x] 3차(넓은 속도범위, 23차원) 완료 + 이 문서 갱신
- [x] 4차(9개 속도 전부, 25차원) 완료 + 이 문서 갱신 — §1 "4차" 결론 1~3 참고
- [x] 5차(`METERS_PER_SPEED_UNIT` 실측 재보정 후 재탐색) 완료 + config.py 배포(`speed15` 활성화)
- [ ] **`ANGLE_RATE_MAX`(조향 변화율 제한, 12도/틱) 시뮬레이터 반영** — 실제 `drive()`가
      마지막에 적용하는 제한인데 시뮬레이터엔 없었음(사용자 확인 요청으로 발견). 결정용
      각도(corner_signal 등)엔 안 걸고 물리 구동(바이시클 모델)에만 적용해야 함 — 실측
      확인 결과 지금까지 찾은 조합 중 약 20%가 이 제한을 초과(최대 21.7°/틱, 12° 한계의
      거의 2배)했음. 반영 후 6차 재탐색 필요.
- [ ] `corner_min_radius_px`의 속도별 감소 경향(§1 4차 결론 3)이 진짜 추세인지
      `--sensitivity`로 재확인 — 게인 스케줄링(파라미터를 속도 함수로 실시간 가변화)
      후보 1순위
- [ ] **2026-08-18 실차 세션에서 측정 예정 2건**:
  - 실측속도 → 정확한 `SPEED_NORMAL` 확정, 그 근처 지점을 시드로 다시 한 번 정밀 탐색
  - **세그멘테이션 노이즈 실측** — `dl_lane`의 프레임간 원시 웨이포인트(EMA 적용 전, 노란선)
    x좌표 표준편차를 로깅으로 계산. 차를 몰 필요는 없고 카메라가 트랙을 보는 상태에서 로그만
    남기면 됨. `NOISE_STD_PX=1.5px` 가정값 검증용 — **우선순위 높음**: 지금 Optuna가 찾은
    `path_ema_alpha`↑/`dx_deadzone_px`↓ 방향(노이즈에 더 민감하게 반응)이, 실제 노이즈가
    가정보다 크면 `e86c023`/`936dcd7`이 잡았던 "좌우 진동"을 다시 불러올 위험이 있음 —
    이 측정 전까지는 `wheelbase_px`/`path_ema_alpha`/`dx_deadzone_px` 추천을 신뢰하지 말 것.
- [ ] 실측 노이즈로 `NOISE_STD_PX` 갱신 후 재탐색
- [ ] `wheelbase_px`/`path_ema_alpha` 상향을 실차에서 별도로(서행 가능한 곳, 즉시 개입
      준비) 검증 — 두 값 다 과거에 "진동"을 이유로 낮췄던 이력이 있어 최우선 확인 대상
- [ ] 최종 값 결정 시 README §0.5.x로 정식 이관 + config.py 반영
