# UMK — 자율주행 트랙 경주 로봇 (국민대 자율주행 경진대회)

## 저장소 = 실차 배포 소스
이 저장소의 각 최상위 폴더(`track_drive/`, `yolo_ros/`, `xycar_device/`)는 실제 로봇의
`~/xycar_ws/src/` 안에 들어가는 ROS2 패키지입니다. 여기서 수정 → 실차에서 pull →
해당 폴더를 `xycar_ws/src/`에 붙여넣고 `colcon build`하는 배포 모델입니다.

## 이 환경엔 ROS2 툴체인이 없음
`ros2`/`colcon` 미설치. 검증은 `python3 -m py_compile <file>`(문법 체크)까지만 가능 —
실제 빌드/노드 실행/토픽 확인은 실차에서만 할 수 있습니다.

## 튜닝값은 `track_drive/track_drive/config.py` 하나로
대부분의 모듈이 `from .config import *`로 가져다 씁니다. 파라미터를 찾거나 바꿀 땐
여기부터 확인하세요.

## 문서화 컨벤션
`track_drive/track_drive/README.md`는 번호 섹션(0~8)마다 날짜·원인·수정·"알려진 한계"를
남기는 살아있는 설계문서입니다. 코드에 기능/버그수정을 넣을 땐 같은 스타일로 README도
갱신하는 게 이 저장소의 관례입니다. 코드 주석도 "무엇"보다 "왜"(설계 배경, 실차 실측
여부, 날짜별 수정 이력)를 한글로 촘촘히 남기는 톤입니다 — 새 코드도 이 톤에 맞출 것.

## 센서 죽음은 크래시가 아니라 "조용한 고정값"으로 나타남
IMU/VESC 등 센서가 죽어있으면 콜백이 안 불려서 해당 변수(`imu_yaw`, `v_mps` 등)가
초기값/직전값에 그대로 고정됩니다 — 에러 로그 없이 "이상하게 값이 안 움직인다"는
증상으로만 드러나니 주의. 이 저장소는 `_vesc_live()` 같은 `_*_t` 타임스탬프 +
`*_STALE_SEC` 가드 패턴으로 생존을 체크합니다 — 새 센서값을 쓸 때도 이 패턴을 따를 것.
